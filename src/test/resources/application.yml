eureka:
  client:
    enabled: false

server:
  port: 0

spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:test;MODE=MySQL;DB_CLOSE_ON_EXIT=FALSE
  main:
    banner-mode: 'off'
  output:
    ansi:
      enabled: ALWAYS
  zipkin:
    enabled: false

url-service:
  shortDomain: a.i
  baseUrl: http://unit.testing.dashaun.io/